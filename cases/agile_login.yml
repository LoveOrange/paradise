- login:
    url: "http://agile.baidu.com"
    actions:
      - scripts: "sh shell/ci.sh"
      # todo add a username
      - fill: "#username(jianglin04)"
      # todo add a password
      - fill: "#password()"
      - click: ".bt-login"
      - sleep: 1
    validate: "title"
    expect:
      - equals: "Agile 敏捷平台"

