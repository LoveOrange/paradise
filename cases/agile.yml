# 测试基本显示
- base_show:
    url: "http://agile.baidu.com/#/builds/appdemo/qa-code/jianglin@trunk"
    actions:
      - sleep: 1
      - click: ".trunk-tab-icon"
    validate: ".trunk-tab-icon"
    expect:
      - pass:
#      - equals: " 主干"

# 测试重新构建
- rebuild:
    url: "http://agile.baidu.com/#/builds/appdemo/qa-code/jianglin@trunk"
    action:
      - sleep: 2
      - click: ".commit-info"
    validate: ".commit-info"
    expect:
      - pass:

# 测试拉分支

# 测试发布
