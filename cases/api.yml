# - ${test_name}
#     url: ${url}
#     method: GET | POST
#     headers:
#       - ${header_name} : ${header_value}
#     params:
#       - ${param_name} : ${param_value}
#     expect:
#       - equals: ${expect_value}

- is_ok:
    url: /ok
    method: get
    expect:
      - equals: "ok"